#include <iostream>
using namespace std;

int main()
{
	cin.tie(nullptr);
	cout.tie(nullptr);
	ios::sync_with_stdio(false);

	while (true)
	{
		int N;
		cin >> N;

		int P, M;
		cin >> P >> M;

		if (P > M)
			cout << P - M << "\n";
		else{
			for (int i = 0; i < P; i++) {

			}
		}
	}
}