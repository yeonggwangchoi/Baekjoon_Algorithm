#include<iostream>
#include<string>
using namespace std;
int main(void)
{
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	int num;
	string str1, str2, str3.;
	cin >> num;

	for (int i = 0; i < num; i++)
	{
		cin >> str1;
		if (str1.size() > str2.size())
		{
			str3.push_pack(str2);
	}

	return 0;
}