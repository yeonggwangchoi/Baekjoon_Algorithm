#include <iostream>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int N, i;

	cin >> N;
	int* n;
	n = new int[N];

	for (i = 0; i < N; i++)
	{
		cin >> n[i];

		if (n[i] % 1 == 0 && n[i] % n[i] == 0 ||)
		{

		}
	}

	return 0;
}
