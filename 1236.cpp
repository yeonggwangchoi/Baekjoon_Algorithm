#include <iostream>
#include <vector>
using namespace std;

int main()
{
	char** mat;
	char N, M;
	cin >> N >> M;
	mat = new char* [N];
	for (int i = 0; i < N; i++)
		mat[i] = new char[M];
	for (int i = 0; i < N; i++)
	{
		for (int j = 0; j < M; j++)
		{
			cin >> mat[i][j];
			if()
		}
	}

	
}
