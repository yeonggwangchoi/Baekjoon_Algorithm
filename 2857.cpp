#include <iostream>
using namespace std;

int main()
{
	cin.tie(nullptr);
	cout.tie(nullptr);
	ios::sync_with_stdio(false);

	for (int i = 0; i < 5; i++) {
		string str;
		cin >> str;

		for (int j = 0; j < str.length(); j++) {

		}
	}
}